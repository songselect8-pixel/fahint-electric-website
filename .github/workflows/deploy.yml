name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - run: npm ci

      # Keep missing or unsafe published assets from reaching GitHub Pages.
      - run: npm test

      # SITE_BASE controls the asset path.
      # - Custom domain (e.g. www.fahint.com via Cloudflare)  -> leave as "/"
      # - GitHub project page (user.github.io/<repo>/)        -> use "/<repo>/"
      - name: Build
        run: npm run build
        env:
          SITE_BASE: ${{ vars.SITE_BASE || '/fahint-electric-website/' }}

      # SPA fallback: any deep link (e.g. /products/gf15) must serve index.html
      - name: Add SPA fallback and disable Jekyll
        run: |
          cp dist/index.html dist/404.html
          touch dist/.nojekyll

      # Custom domain: set the CNAME repository variable to e.g. www.fahint.com
      - name: Write CNAME
        if: vars.CUSTOM_DOMAIN != ''
        run: echo "${{ vars.CUSTOM_DOMAIN }}" > dist/CNAME

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
